# LAN-air-hockey
name is... self explanitory

Boilerplate convention:
- "boilerplate" means only base code: minimal `head` (meta, title, links, scripts) and an empty `body`. Do not place content inside the `body` in boilerplate files.
